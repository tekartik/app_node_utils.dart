name: tekartik_app_node_build
description: Node build dependencies
version: 0.1.0
deploy_to: none

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  path: ^1.7.0

  pedantic: ^1.9.0
  test: ^1.14.4
  build_runner:
  build_node_compilers:
  build_test:
  process_run:

